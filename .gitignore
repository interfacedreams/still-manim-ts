node_modules
dist
.vite
*.log
.DS_Store

# Regenerated by `npm run outputs` — every run renames numbered SVGs, so git
# noise scales with scene count. Local-only artifacts.
outputs/python/
outputs/typescript/
outputs/index.html
